apiVersion: marlin/v1
kind: Registry

packages:
  - name: niji
    version: 1.1.1
    description: Deterministic color palette engine — web-safe and archived Atlas naming, Cockpit provider runtime, harmony schemes, and perceptual ramps
    manifest: manifests/niji/1.1.1.yaml
    tags:
      - color
      - design
      - palette
      - ui
  - name: cairn
    version: 0.1.3
    description: Local-first operational registry for assets, releases, deployments, and secret-store references (KeePassXC by default)
    manifest: manifests/cairn/0.1.3.yaml
    tags:
      - cairn
      - registry
      - secrets-reference
      - sqlite
