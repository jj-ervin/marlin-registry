# ONS Conformance Profile
# Generated by dxp-init v{{validatorVersion}} on {{timestamp}}
# Standard: ONS v1.3.0 / GC:2009 (DEV-ACCORD.01)
# Claim scope: namespace and naming configuration only.
# This is not a compliance certificate of any kind.
version: "1.0.0"
project: "{{projectName}}"
namespace_root: "{{namespaceRoot}}"
product_root: "{{productRoot}}"
validation_tier: "{{onsValidationTier}}"
separator_preference: "{{separatorPreference}}"
{{#if vscodeExtensionNaming}}vscode_extension_naming: "{{vscodeExtensionNaming}}"
{{/if}}{{#if tokenPrefixPattern}}token_prefix_pattern: "{{tokenPrefixPattern}}"
{{/if}}claim_scope: "namespace-configuration-only"
does_not_certify:
  - regulatory_compliance
  - security_control_effectiveness
  - eu_ai_act_conformity
  - soc2_readiness
  - nist_ai_rmf_completion
principal: "{{principal.principal_id}}"
provisioned_at: "{{timestamp}}"
