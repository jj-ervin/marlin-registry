# GC Principal Identity Record
# Generated by dxp-init v{{validatorVersion}} on {{timestamp}}
# Schema: https://governancecommons.org/schemas/gc-principal.schema.json
# This file is the authority anchor for all governance records in this project.
version: "1.0.0"
principal_id: "{{principal.principal_id}}"
name: "{{principal.name}}"
{{#if principal.email}}email: "{{principal.email}}"
{{/if}}{{#if principal.github_handle}}github_handle: "{{principal.github_handle}}"
{{/if}}role: "{{principal.role}}"
entity:
  type: "{{principal.entity.type}}"
{{#if principal.entity.name}}  name: "{{principal.entity.name}}"
{{/if}}{{#if principal.entity.jurisdiction}}  jurisdiction: "{{principal.entity.jurisdiction}}"
{{/if}}{{#if principal.succession}}succession:
  name: "{{principal.succession.name}}"
  handle: "{{principal.succession.handle}}"
{{#if principal.succession.email}}  email: "{{principal.succession.email}}"
{{/if}}{{/if}}attestation:
  confirmed: true
  timestamp: "{{timestamp}}"
  statement: "I confirm that I have the authority to install governance on this repository and that the information above is accurate."
created_at: "{{timestamp}}"
