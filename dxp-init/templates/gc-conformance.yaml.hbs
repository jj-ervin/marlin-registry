# GC Conformance Declaration
# Generated by dxp-init v{{validatorVersion}} on {{timestamp}}
#
# IMPORTANT: This file documents governance adoption intent and scaffold state.
# It is evidence scaffolding, not a compliance certificate.
# Actual compliance with any regulation requires operational controls,
# security review, legal scope confirmation, and in some cases external audit.
version: "1.0.0"
project: "{{projectName}}"
conformance_level: "{{gcConformanceLevel}}"
authority_level: "{{authorityLevel}}"
principal: "{{principal.principal_id}}"
{{#if principal.entity.name}}entity: "{{principal.entity.name}}"
{{/if}}declared_at: "{{timestamp}}"
standards:
  - id: "GC:2008"
    name: "DEV-ACCORD.00 — Planning Document Standard"
    status: "{{#if emitPlanningDocs}}adopted{{else}}not-adopted{{/if}}"
  - id: "GC:2009"
    name: "DEV-ACCORD.01 — ONS Registration for Portfolio Doc Types"
    status: "adopted"
  - id: "ONS-1.3.0"
    name: "Ontic Namespace Structure v1.3.0"
    status: "adopted"
{{#if agenticGovernance}}  - id: "GEE-0.1"
    name: "Governed Event Envelope v0.1"
    status: "adopted"
{{/if}}{{#if preset}}evidence_preset: "{{preset}}"
{{/if}}
